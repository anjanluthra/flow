# Couple photos

Drop your two wedding photos here so they appear in Flow:

- `login.jpg` — shown on the left panel of the sign-in page
- `hero.jpg`  — shown in the banner on the Home page

Any landscape/portrait JPG or PNG works. If a file is missing, Flow falls
back to an elegant gradient, so the pages always look finished.
